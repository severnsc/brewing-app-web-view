import React from "react"

const ActiveTimerContainer = () => (
	<div style={{border: "1px solid black"}}>
		<h2>Active Timer</h2>
		<p>You don't have any active timers!</p>
	</div>
)

export default ActiveTimerContainer